describe("EditImgBtn Component", () => {
  // 1. 컴포넌트가 정상적으로 렌더링되는지 테스트
  test("should render add button with correct icon and style", () => {
    // - `add` 타입의 버튼 렌더링
    // - 버튼에 `plus-gray.svg` 아이콘이 포함되어 있는지 확인
    // - `BUTTON_STYLE.add` 스타일이 적용되었는지 확인
  });

  test("should render edit button with correct icon and style", () => {
    // - `edit` 타입의 버튼 렌더링
    // - 버튼에 `edit.svg` 아이콘이 포함되어 있는지 확인
    // - `BUTTON_STYLE.edit` 스타일이 적용되었는지 확인
  });

  // 2. 버튼 클릭 시 `onClick` 이벤트가 실행되는지 테스트
  test("should trigger onClick event when button is clicked", () => {
    // - `mock` 함수를 생성하여 `onClick` 핸들러로 전달
    // - 버튼 클릭 후 `onClick`이 실행되었는지 확인
    // - `onClick`이 한 번만 호출되었는지 검증
  });
});
